import { View, Text } from "react-native";
import React from "react";

type Props = {};

const StyledText = (props: Props) => {
  return <Text className=" bg-red-600">StyledText</Text>;
};

export default StyledText;
