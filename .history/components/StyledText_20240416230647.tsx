import { View, Text } from "react-native";
import React from "react";

type Props = {};

const StyledText = (props: Props) => {
  return <Text className="font-Inter text-">StyledText</Text>;
};

export default StyledText;
