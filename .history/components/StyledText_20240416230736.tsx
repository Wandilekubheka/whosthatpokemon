import { View, Text } from "react-native";
import React from "react";

type Props = {};

const StyledText = (props: Props) => {
  return <Text className=" text-gray-800">StyledText</Text>;
};

export default StyledText;
