import { View, Text } from "react-native";
import React from "react";

type Props = {};

const StyledText = (props: Props) => {
  return (
    <Text className="font-Inter text-gray-800 dark:text-white">StyledText</Text>
  );
};

export default StyledText;
